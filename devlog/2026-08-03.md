# 开发日志 - 2026-08-03

## 完成事项
- [x] 增加“全部 / 文字 / 图片”类型筛选，分页与总数按当前类型计算
- [x] 修正搜索分页总数沿用全部记录总数的问题
- [x] 增加截图库管理模式：多选、全选已加载、批量删除
- [x] 增加单张和批量“复制为文件”，可在 Windows 资源管理器中粘贴 PNG
- [x] 图片批量删除同步清理数据库记录和本地图片文件
- [x] 搜索高亮改用安全 DOM 文本节点，避免剪贴板 HTML 被解析
- [x] 增加图片列表/网格排布切换，并持久化用户选择
- [x] 网格在默认窗口三列、最小窗口两列，缩略图完整显示不裁剪
- [x] 增加单张图片离线文字识别，识别成功后直接复制中英文文字
- [x] OCR 仅按用户操作执行，不保存识别结果，也不参与搜索
- [x] OCR worker 空闲 60 秒后自动释放，避免长期占用内存
- [x] 发布版本提升为 v1.1.0，生成 Windows x64 NSIS 安装程序
- [x] 修正 OCR 冒烟测试失败时仍返回退出码 0 的问题，并隔离测试缓存目录

## 验证
- [x] 修改过的 JavaScript 文件通过 `node --check`
- [x] 数据库筛选、搜索计数和批量删除通过内存测试
- [x] PowerShell Base64 + JSON 标准输入可正确传递中文绝对路径
- [x] `npm.cmd run build:dir` 目录打包成功，新增文件已进入 `app.asar`
- [x] `image_layout` 默认值与 list/grid 持久化通过内存数据库测试
- [x] 最新排布版本已打包到 `outputs/layout-build/win-unpacked`
- [x] 用户已在桌面环境验证上一轮图片筛选、批量管理和文件粘贴功能
- [x] OCR 新增 JavaScript 文件通过 `node --check`
- [x] OCR 冒烟测试同时识别出内存图片中的中文和英文
- [x] 优化目录版打包成功，成品解包 worker 可加载本地双语言模型
- [x] 打包成品从 `app.asar` 首次复制模型并实际识别中英文通过
- [x] 修复真实窗口 OCR 失败的边缘条件：改用图片 Buffer、隔离进度回传异常、worker 失败自动重试一次
- [x] 修复版对截图库最近 20 张图片验证：19 张成功、1 张正确返回无文字、0 张异常
- [x] 修复版完整 IPC 验证：真实图片识别 174 字、24 次进度回传、剪贴板写入成功
- [x] 根据正式 EXE 报错恢复 `tesseract-core` 普通/SIMD/Relaxed SIMD 运行核心
- [x] 增加 `--ocr-smoke-test` 成品自检，避免测试从项目 `node_modules` 兜底加载依赖
- [x] V2 正式 EXE 自检退出码 0，确认成品可独立加载 OCR 核心并执行识别
- [x] OCR 成品增量约 17.3MB，未使用的完整模型与非 LSTM 核心已排除
- [x] `npm audit --omit=dev`：生产依赖 0 项漏洞
- [x] v1.1.0 安装包构建成功，版本元数据为 1.1.0，体积约 91.4MB
- [x] v1.1.0 成品 `--ocr-smoke-test` 退出码 0，安装包 7-Zip 完整性检查通过
- [ ] 在 320×400 最小窗口验证批量工具栏布局
- [ ] 在桌面环境验证列表/网格排布切换

## 待办事项
- [ ] Phase 7.6：实现启动时和每小时自动清理
- [ ] Phase 7.7-7.8、7.10：补齐状态、动画和完整验收

## 备注
- 文件粘贴通过 STA PowerShell 写入标准 Windows FileDropList，不新增原生依赖
- 图片视图不执行关键词搜索，按最近复制时间浏览
- OCR 使用 Tesseract.js 7.0.0 与本地 `chi_sim` / `eng` 精简模型，首次使用写入约 4.7MB 模型目录
- 按当前需求不支持图片文字搜索，因此未修改数据库结构
- 原 `dist/win-unpacked` 被正在运行的应用锁定，本轮未主动结束用户进程，改用独立输出目录验证
- v1.1.0 安装包位于 `outputs/v1.1.0/Clipboard History Setup 1.1.0.exe`
